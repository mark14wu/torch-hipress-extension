# How to install

```shell
# create virtual env
virtualenv --python=python3 venv
# activate
source venv/bin/activate
# install dependencies
pip3 install torch==1.3.0
# install lltm
bash install.sh
```